import React from 'react';

import { default as QuoteComponent } from '../components/Quote';

const Quote: React.FunctionComponent<{}> = props => {
	return <QuoteComponent />;
};

export default Quote;
